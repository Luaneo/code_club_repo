#include <string>

using namespace std;

int main()
{
    int a, b, c;
    a = 1;
    b = 2;
    c = a + b;

    float d = a / b;
    float e = float(a) / float(b);

    string hello = "Hello";
    string world = "world";
    string s = hello + ", " + world + "!";
}
